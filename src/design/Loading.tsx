import styled from 'styled-components';

export const Loading = () => <LoadingComponent>로딩중이에요.</LoadingComponent>;

const LoadingComponent = styled.div`
  font-size: 1.5rem;
  text-align: center;
`;
